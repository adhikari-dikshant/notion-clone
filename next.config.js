/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    images: {
        domains: ['wotsremgkrxwealebvnm.supabase.co']
    }
}
module.exports = nextConfig
